# Calo Capital

## Contact Form

The contact form is a native HTML form submitted directly to FormSubmit:

`https://formsubmit.co/protection@calocapital.io`

It requires no backend handler, API key, or environment variables. After submission, FormSubmit redirects to `/contact?submitted=true` and the page displays a confirmation message.

